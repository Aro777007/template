#include <iostream>
using namespace std;

template <typename t>
t faktorial(t a)
{
	if (a >= 1)
	{
		return a * faktorial(a - 1);
	}
	
}




int main()
{
	int a = 0;
	cin >> a;
	int result = faktorial(a);
	faktorial <int> (a);
	cout << result ;
   
}
